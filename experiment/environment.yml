name: expt
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8  # Specify your Python version here
  - pandas
  - psychopy  # psychopy is available through conda-forge
  - pip
  - pip:
      # List any pip-only packages here. If your custom modules have dependencies not available in Conda, specify them here.
      # Example:
      # - some-pip-package==1.2.3


